---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    # Uses same as updater
    iam.gke.io/gcp-service-account: testgrid-canary@k8s-testgrid.iam.gserviceaccount.com
  name: summarizer
  namespace: testgrid-canary
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: testgrid-summarizer-tabs
  namespace: testgrid-canary
  labels:
    app: testgrid
    channel: stable
    component: summarizer-tabs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testgrid
      channel: stable
      component: summarizer-tabs
  template:
    metadata:
      labels:
        app: testgrid
        channel: stable
        component: summarizer-tabs
    spec:
      serviceAccountName: summarizer
      containers:
      - name: summarizer
        image: gcr.io/k8s-testgrid/summarizer:v20220323-v0.0.124-47-g734ed81
        ports:
        - name: metrics
          containerPort: 2112
        args:
        - --config=gs://k8s-testgrid-canary/config
        - --confirm
        - --grid-path=unused
        - --json-logs
        - --persist-queue=gs://k8s-testgrid-canary/queue/summarizer-tabs.json
        - --pubsub=k8s-testgrid/canary-tab-updates
        - --summary-path=summary
        - --tab-path=tabs
        - --wait=1h
        resources:
          requests:
            cpu: "1"
            memory: "50G"
          limits:
            cpu: "2"
            memory: "100G"
---
